

JVM needs argument

    -enableassertions
       or
    -ea
    
    
@Test(.. )  expected=Exception.className  timeout(Xms)
@Before
@After
@BeforeClass  must be static
@AfterClass   must be static
@Ignore





JUnit assert methods
    http://www.vogella.com/tutorials/JUnit/article.html
    throws AssertionExcepiton on failure
    
import org.junit.Test;
import static org.junit.Assert.*;


assert
    
fail(String)
assertTrue/false([msg], condition)

assertEquals([String message], expected, actual)
assertEquals([String message], expected, actual, tolerance)   tolerance=number of decimals to use

assertNull([message], object)

assertNotNull([message], object)

assertSame([String], expected, actual)

assertNotSame([String], expected, actual)









    
    
           