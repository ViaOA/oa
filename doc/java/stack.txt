

    private Stack<OAFilter> stack = new Stack<OAFilter>();

        if (stack.size() != 1) throw new Exception("parse failed, expected one filter, but has "+stack.size());

        return stack.pop();

            stack.push(f);

    
    