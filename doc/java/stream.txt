
qqqqqq
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        for (;;) {
            System.out.print("Enter command or 'h'elp: ");
            String cmd = br.readLine();
        }




// convert a String with NL to a buffered stream
    BufferedReader br = new BufferedReader(new StringReader(s2));
    for ( ;; ) {
        String sx = br.readLine();
        if (sx == null) break;
        s += OAString.NL + getCommentString() + sx; 
    }
qqqqqqqqqqqqq

    BufferedReader br = new BufferedReader(new StringReader(s));
    for ( ;; ) {
        try {
            s = br.readLine();
            if (s == null) break;
        }
        catch (Exception e) {
            break;
        }
        String s1 = OAString.field(s, ":", 1).trim();
        String s2 = OAString.field(s, ":", 2, 999).trim();
        if (s1.equalsIgnoreCase("version")) {
            if (label != null) label.setText(s2);
        }
        cellInfo.addToolTipText("  "+s1+": "+s2);
        
    }
