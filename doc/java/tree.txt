

    private TreeMap<Short, Integer> tmFunctions = new TreeMap<Short, Integer>();

    
    
    
            short fid = im.getFunctionId();
            Object objx = tmFunctions.get(fid);
            if (objx == null) {
                tmFunctions.put(fid, 1);
            }
            else {
                tmFunctions.put(fid, ((Integer)objx).intValue()+1);
            }
            
            System.out.println(String.format("%,d) %s", i + 1, im.getFunctionName()));


        int cnt = 0;
        for (short key : tmFunctions.keySet()) {
            int amt = tmFunctions.get(key);
            System.out.println(String.format("%d) %s %,d",++cnt, ICEMessageDelegate.getFunctionName(key), amt));
        }
        //for (Map.Entry<Integer,Integer> entry = tmFunctions.firstEntry(); entry != null; tmFunctions.higherEntry(null) 
    