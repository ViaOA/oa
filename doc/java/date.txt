

Calendar cal = Calendar.getInstance();


See: format.txt

        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd-HHmmss");



exam.getDate(),OADateTime.getFormat(java.text.DateFormat.LONG)

> 
    SimpleDateFormat fmt = new SimpleDateFormat("MM/dd HH:mm:ss.SSS");
    fmt.format(new Date(_receiveBeginTime))



    Date beginDate = new Date(101, 0, 1, 0, 0, 0);  // Jan 1, 2001
    
    Calendar cal = new GregorianCalendar(TimeZone.getTimeZone("GMT"), Locale.getDefault());
    Calendar cal = Calendar.getInstance();
    cal.setTime(beginDate);
    cal.set(Calendar.MILLISECOND, 0);
    beginDate = cal.getTime();



qqqqqqq

        long msNow = System.currentTimeMillis();
        long msSingleDay = 24 * 60 * 60 * 1000;
        long msTime = msNow - (ICE2SF.NumberOfLoginDays * msSingleDay);
        
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
        String sDate = df.format(new Date(msTime));
        sDate += "T23:01:01Z";

        
qqqqqqqq
    int _time_utc_offset;   //timezone offset between Local & UTC time
    TimeZone timezone = TimeZone.getTimeZone( "GMT" +  _time_utc_offset ); // e.g. GMT-09:00
    calendar.setTimeZone( timezone );
    final SimpleDateFormat sdf = new SimpleDateFormat( _SQL_FORMAT_DEFAULT );
    sdf.setTimeZone(TimeZone.getTimeZone("GMT" +  _time_utc_offset));
        