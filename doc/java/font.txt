

http://mindprod.com/jgloss/font.html


> list all fonts

    import java.awt.GraphicsEnvironment;
    ...
    public class ShowFonts
       {
       public static void main ( String [] args )
          {
          GraphicsEnvironment ge = GraphicsEnvironment.getLocalGraphicsEnvironment();
          String[] names = ge.getAvailableFontFamilyNames();
          for ( int i=0; i<names.length; i++ )
             {
             System.out.println( names[i] );
             }
          }
       }
       
   
   
// now deprecated
String[] files = java.awt.Toolkit.getDefaultToolkit().getFontList();
   
   
   
   
qqqqq create an Icon gif    
            // icons
            OAImageCreator imgc = new OAImageCreator();
            Graphics2D g = (Graphics2D) imgc.getGraphics(new Dimension(20,20));
            imgc.setTransparentColor(Color.green);
            g.setColor(Color.green);
            g.fillRect(0, 0, 20, 20);
            
            g.setColor(Color.white);
            g.fillRect(2, 2, 16, 16);
            
            g.setColor(Color.darkGray);
            g.draw3DRect(3, 3, 14, 14, false);
            
            Font font = new Font("TimesRoman", Font.BOLD, 12);
            g.setFont(font);
            
            FontMetrics fontMetrics = g.getFontMetrics();
            String letter = ""+objectDef.getName().charAt(0);
            int width = fontMetrics.stringWidth(letter);

            // shadow
            /*
            Color c = colors[i%colors.length].darker();
            g.setColor(c);
            g.drawString(letter, (10-width/2)+1, 15+1);
            */
            Color c = colors[i%colors.length];
            g.setColor(c);
            
            g.drawString(letter, 10-width/2, 15);
            g.setStroke(new BasicStroke(2f));

            //c = colors[(i+1)%colors.length];
            //g.setColor(c);
            //g.setStroke(new BasicStroke(1f));
            //g.drawLine(4, 16, 16, 16);
            
            try {
                String s = OAString.makeFirstCharLower(objectDef.getName());
                imgc.createGif(dirJfc+"/image/"+s+".gif");
                s = OAString.makePlural(OAString.makeFirstCharLower(objectDef.getName()));
                imgc.createGif(dirJfc+"/image/"+s+".gif");
            }
            catch (Exception e) {
                System.out.println("Error creating image. "+e);
            }
               