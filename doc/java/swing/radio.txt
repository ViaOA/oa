
>> yes/no
        panel.add(new JLabel("Reminder:"), gc);
        bg = new ButtonGroup();
        rad = new OARadioButton(getHub(), "reminder", true);
        rad.setEnabled(ModelDelegate.getUserAccessHub(), UserAccess.PROPERTY_IsAdmin);
        rad.setText("Yes");
        bg.add(rad);
        panel.add(rad,gc);

        rad = new OARadioButton(getHub(), "reminder", false);
        rad.setEnabled(ModelDelegate.getUserAccessHub(), UserAccess.PROPERTY_IsAdmin);
        rad.setUseNull(true);
        rad.setText("No");
        bg.add(rad);
        gc.gridwidth = gc.REMAINDER;
        panel.add(rad,gc);
        gc.gridwidth = 1;


>>> more then 2

        panel.add(new JLabel("Severity:"), gc);
        bg = new ButtonGroup();
        rad = new OARadioButton(getHub(), "severity", new Integer(0));
        rad.setEnabled(ModelDelegate.getUserAccessHub(), UserAccess.PROPERTY_IsAdmin);
        rad.setUseNull(true);
        rad.setText("Mild");
        bg.add(rad);
        panel.add(rad,gc);

        rad = new OARadioButton(getHub(), "severity", new Integer(5));
        rad.setText("Medium");
        rad.setEnabled(ModelDelegate.getUserAccessHub(), UserAccess.PROPERTY_IsAdmin);
        bg.add(rad);
        panel.add(rad,gc);

        rad = new OARadioButton(getHub(), "severity", new Integer(10));
        rad.setText("Severe");
        rad.setEnabled(ModelDelegate.getUserAccessHub(), UserAccess.PROPERTY_IsAdmin);
        bg.add(rad);
        gc.gridwidth = gc.REMAINDER;
        panel.add(rad,gc);
        gc.gridwidth = 1;
