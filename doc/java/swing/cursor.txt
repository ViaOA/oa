


    Toolkit tk = Toolkit.getDefaultToolkit();
    try {
        Image img = Resource.getJarIcon("AutoCheckCursor.gif").getImage();
        cursorAutoSelect = tk.createCustomCursor(img, new Point(8,23), "AUTOSELECT");
        
        img = Resource.getJarIcon("SelectCursor.gif").getImage();
        cursorSelect = tk.createCustomCursor(img, new Point(8,2), "SELECT");
    }
    catch (Exception e) {
        System.out.println("====> "+e);
    }

    getTemplatePanel().setCursor(cursorSelect);


qqqqqqqqq        
        
setCursor(Cursor.getPredefinedCursor(Cursor.WAIT_CURSOR));

setCursor(Cursor.getPredefinedCursor(Cursor.DEFAULT_CURSOR));


RootPaneContainer root = 
      (RootPaneContainer)component.getTopLevelAncestor();
    root.getGlassPane().setCursor(WAIT_CURSOR);
    root.getGlassPane().setVisible(true);

 RootPaneContainer root = 
      (RootPaneContainer)component.getTopLevelAncestor();
    root.getGlassPane().setCursor(DEFAULT_CURSOR);
    root.getGlassPane().setVisible(false);
        