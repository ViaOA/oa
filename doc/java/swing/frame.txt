
> move window to top left of mouse location
        PointerInfo pi = MouseInfo.getPointerInfo();
        Point pt = pi.getLocation();
        pt.x -= 32;
        pt.y -= 12;
        frm.setLocation(pt);

>> center window at mouse location        
        Dimension d = frm.getSize();
        PointerInfo pi = MouseInfo.getPointerInfo();
        Point pt = pi.getLocation();
        pt.x -= d.width/2;
        pt.y -= d.height/2;
        frm.setLocation(pt);


>> map key
        OATree tree = ui.createTree();
        String cmd = "esc";
        tree.getInputMap(JComponent.WHEN_IN_FOCUSED_WINDOW).put(KeyStroke.getKeyStroke(KeyEvent.VK_ESCAPE, 0 ,false), cmd);
        tree.getActionMap().put(cmd, new AbstractAction() {
            public void actionPerformed(ActionEvent e) {
                System.exit(0);
            }
        });
        
        
        
>> ===================== Frame

        JFrame frm = new JFrame();
        frm.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        JMenuBar menuBar = new JMenuBar();
        JMenu menu = new JMenu("Item Category");
        menu.add(ui.createNewMenuItem());
        menu.add(ui.createDeleteMenuItem());
        menuBar.add(menu);
        
        frm.setJMenuBar(menuBar);
        
        OATree tree = ui.createTree();
        String cmd = "esc";
        tree.getInputMap(JComponent.WHEN_IN_FOCUSED_WINDOW).put(KeyStroke.getKeyStroke(KeyEvent.VK_ESCAPE, 0 ,false), cmd);
        tree.getActionMap().put(cmd, new AbstractAction() {
            public void actionPerformed(ActionEvent e) {
                System.exit(0);
            }
        });
        
        frm.setLayout(new BorderLayout());
        frm.add(new JScrollPane(tree), BorderLayout.WEST);

        JToolBar toolBar = new JToolBar();
        toolBar.setBorderPainted(false);
        toolBar.setFloatable(true);
        toolBar.setRollover(true);
        
        OACheckBox chk = new OACheckBox(ModelDelegate.getUserAccessHub(), UserAccess.PROPERTY_IsAdmin);
        chk.setText("Admin access");
        toolBar.add(chk);
        frm.add(new OAScroller(toolBar), BorderLayout.NORTH);
        
        frm.add(new JScrollPane(ui.getList()), BorderLayout.EAST);

        frm.add(ui.getListPanel(), BorderLayout.SOUTH);

        frm.add(new JScrollPane(ui.getEditPanel()), BorderLayout.CENTER);
        
        frm.pack();
        
        Dimension d = frm.getSize();
        PointerInfo pi = MouseInfo.getPointerInfo();
        Point pt = pi.getLocation();
        pt.x -= d.width/2;
        pt.y -= d.height/2;
        frm.setLocation(pt);
        // frm.setLocation(new Point(40, 40));
        // frm.setLocation(new Point(1740, 210));
        frm.setVisible(true);
        
        