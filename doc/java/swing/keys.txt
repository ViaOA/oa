

        String cmd = "esc";
        tree.getInputMap(JComponent.WHEN_IN_FOCUSED_WINDOW).put(KeyStroke.getKeyStroke(KeyEvent.VK_ESCAPE, 0 ,false), cmd);
        tree.getActionMap().put(cmd, new AbstractAction() {
            public void actionPerformed(ActionEvent e) {
                dlgItem.setVisible(false);
            }
        });



>> key map
        OATree tree = ui.createTree();
        String cmd = "esc";
        
        tree.getInputMap(JComponent.WHEN_IN_FOCUSED_WINDOW).put(KeyStroke.getKeyStroke(KeyEvent.VK_ESCAPE, 0 ,false), cmd);
        tree.getActionMap().put(cmd, new AbstractAction() {
            public void actionPerformed(ActionEvent e) {
                System.exit(0);
            }
        });

        
>> enter key -         
        // create [Enter] key accelerator key for search button - needs to be on keyRelease=true so that txtFields dont consume it
        panSearch.getInputMap(JComponent.WHEN_ANCESTOR_OF_FOCUSED_COMPONENT).put(KeyStroke.getKeyStroke(KeyEvent.VK_ENTER, 0, true), "cmd");
        panSearch.getActionMap().put("cmd", new AbstractAction() {
            public void actionPerformed(ActionEvent e) {
                onSearch();
            }
        });            





        