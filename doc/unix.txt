

bash

DEBUG_PORT=5000
DEBUG_OPTIONS="-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=y,address=${DEBUG_PORT}"
# DEBUG_OPTIONS=""

GC_LOG="${INSTALL_DIR}/logs/LoginTest_gc.log"
GC_FLAGS="-verbose:gc -XX:+PrintGCDateStamps -XX:+PrintGCDetails -Xloggc:${GC_LOG}"

echo "starting ... now"
${JAVA_HOME}/bin/java -server -d64 -Xms256m -Xmx1200m -cp jar/\* ${DEBUG_OPTIONS} ${GC_FLAGS} com.theice.mr.gs.client.ClientTest $@
